pluginManagement {
    repositories {
        maven{
            setUrl("https://maven.aliyun.com/repository/public/")
        }
        mavenCentral() // 如果需要，也可以添加 Maven Central
    }
}
rootProject.name = "demo2"
